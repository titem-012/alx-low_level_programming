#include "main.h"
/**
 * _isupper - check if uppercase
 * @c: Input
 * Return: 1 if its uppercase,or else 0
 */

int _isupper(int c)
{
if (c >= 'A' && c <= 'Z')
{
return (1);
}
else
{
return (0);
}
}
